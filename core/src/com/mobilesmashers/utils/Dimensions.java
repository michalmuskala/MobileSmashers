package com.mobilesmashers.utils;

public class Dimensions {
	public static final int
			logoHeight = 950,
			logoWidth = 250,
			logoBegX = Constants.APP_WIDTH / 2 - logoHeight / 2,
			logoBegY = Constants.APP_HEIGHT - 270,

			playHeight = 380,
			playWidth = 150,
			playBegX = Constants.APP_WIDTH / 2 - playHeight / 2,
			playBegY = Constants.APP_HEIGHT / 2 - playWidth,

			infoHeight = 380,
			infoWidth = 150,
			infoBegX = Constants.APP_WIDTH / 2 - infoHeight / 2,
			infoBegY = Constants.APP_HEIGHT / 3 - infoWidth,

			volumeLowerHeight = 120,
			volumeLowerWidth = 120,
			volumeLowerBegX = 4 * Constants.APP_WIDTH / 6,
			volumeLowerBegY = Constants.APP_HEIGHT / 20,

			volumeHigherHeight = 120,
			volumeHigherWidth = 120,
			volumeHigherBegX = 6 * Constants.APP_WIDTH / 7,
			volumeHigherBegY = Constants.APP_HEIGHT / 20,

			movingPlayerInfoBegX = Constants.APP_WIDTH / 2 - infoHeight / 2,
			movingPlayerInfoBegY = Constants.APP_HEIGHT - 270,

			primeWidth = 380,
			primeHeight = 150,
			primeBegX = Constants.APP_WIDTH / 15,
			primeBegY = Constants.APP_HEIGHT / 7,

			evenWidth = 380,
			evenHeight = 150,
			evenBegX = (int) (5.5 * Constants.APP_WIDTH / 15),
			evenBegY = Constants.APP_HEIGHT / 7,

			tenthWidth = 380,
			tenthHeight = 150,
			tenthBegX = 10 * Constants.APP_WIDTH / 15,
			tenthBegY = Constants.APP_HEIGHT / 7,

			gameOverWidth = 200,
			gameOverHeight = 630,
			gameOverBegX = Constants.APP_WIDTH / 2 - gameOverHeight / 2,
			gameOverBegY = Constants.APP_HEIGHT - 250,

			menuWidth = 150,
			menuHeight = 430,
			menuBegX = Constants.APP_WIDTH / 2 - menuHeight / 2,
			menuBegY = Constants.APP_HEIGHT / 3 - menuWidth;
}
